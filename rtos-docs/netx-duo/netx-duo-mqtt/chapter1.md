---
title: 'Capítulo 1: Introducción a MQTT de NetX Duo para Azure RTOS'
description: El paquete de cliente MQTT de NetX Duo requiere que NetX Duo (versión 5.10 o posterior) esté instalado y configurado correctamente y que se haya creado la instancia de IP.
author: philmea
ms.author: philmea
ms.date: 05/19/2020
ms.topic: article
ms.service: rtos
ms.openlocfilehash: 2e13b997f987e2fd82569bcb1904218908313d70
ms.sourcegitcommit: e3d42e1f2920ec9cb002634b542bc20754f9544e
ms.translationtype: HT
ms.contentlocale: es-ES
ms.lasthandoff: 03/22/2021
ms.locfileid: "104814661"
---
# <a name="chapter-1---introduction-to-azure-rtos-netx-duo-mqtt"></a><span data-ttu-id="3997f-103">Capítulo 1: Introducción a MQTT de NetX Duo para Azure RTOS</span><span class="sxs-lookup"><span data-stu-id="3997f-103">Chapter 1 - Introduction to Azure RTOS NetX Duo MQTT</span></span>

## <a name="netx-duo-mqtt-requirements"></a><span data-ttu-id="3997f-104">Requisitos de MQTT de NetX Duo</span><span class="sxs-lookup"><span data-stu-id="3997f-104">NetX Duo MQTT Requirements</span></span>

<span data-ttu-id="3997f-105">El paquete de cliente MQTT de NetX Duo Azure RTOS requiere que NetX Duo (versión 5.10 o posterior) esté instalado y configurado correctamente y que se haya creado la instancia de IP.</span><span class="sxs-lookup"><span data-stu-id="3997f-105">The Azure RTOS NetX Duo MQTT client package requires that NetX Duo (version 5.10 or later) be installed, properly configured, and the IP instance has been created.</span></span> <span data-ttu-id="3997f-106">El módulo TCP debe estar habilitado en el sistema.</span><span class="sxs-lookup"><span data-stu-id="3997f-106">The TCP module must be enabled in the system.</span></span> <span data-ttu-id="3997f-107">Además, si se requiere seguridad de TLS, el módulo NetX Secure TLS debe configurarse según el parámetro de seguridad requerido por el agente.</span><span class="sxs-lookup"><span data-stu-id="3997f-107">In addition, if TLS security is required, the NetX Secure TLS module needs to be configured according to the security parameter required by the broker.</span></span>

## <a name="netx-duo-mqtt-specification"></a><span data-ttu-id="3997f-108">Especificación de MQTT de NetX Duo</span><span class="sxs-lookup"><span data-stu-id="3997f-108">NetX Duo MQTT Specification</span></span>

<span data-ttu-id="3997f-109">La implementación del cliente MQTT de NetX Duo es compatible con la versión 3.1.1 de MQTT de OASIS del <sup>29 de octubre de 2014</sup>.</span><span class="sxs-lookup"><span data-stu-id="3997f-109">NetX Duo MQTT client implement is compliant with OASIS MQTT Version 3.1.1 Oct 29<sup>th</sup> 2014.</span></span> <span data-ttu-id="3997f-110">La especificación se puede encontrar en:</span><span class="sxs-lookup"><span data-stu-id="3997f-110">The specification can be found at:</span></span>

- [http://mqtt.org/](http://mqtt.org/)

## <a name="netx-duo-mqtt-basic-operation"></a><span data-ttu-id="3997f-111">Operación básica de MQTT de NetX Duo</span><span class="sxs-lookup"><span data-stu-id="3997f-111">NetX Duo MQTT Basic Operation</span></span>

<span data-ttu-id="3997f-112">MQTT (transporte de telemetría de cola de mensajes) se basa en el modelo de publicador-suscriptor.</span><span class="sxs-lookup"><span data-stu-id="3997f-112">MQTT (Message Queue Telemetry Transport) is based on publisher-subscriber model.</span></span> <span data-ttu-id="3997f-113">Un cliente puede publicar información en otros clientes mediante un agente.</span><span class="sxs-lookup"><span data-stu-id="3997f-113">A client can publish information to other clients through a broker.</span></span> <span data-ttu-id="3997f-114">Un cliente, si está interesado en un tema, puede suscribirse al tema por medio del agente.</span><span class="sxs-lookup"><span data-stu-id="3997f-114">A client, if interested in a topic, can subscribe to the topic through the broker.</span></span> <span data-ttu-id="3997f-115">Un agente es responsable de entregar los mensajes publicados a sus clientes que se suscriben al tema.</span><span class="sxs-lookup"><span data-stu-id="3997f-115">A broker is responsible for delivering published messages to its clients who subscribe to the topic.</span></span> <span data-ttu-id="3997f-116">En este modelo de publicador-suscriptor, varios clientes pueden publicar datos con el mismo tema.</span><span class="sxs-lookup"><span data-stu-id="3997f-116">In this publisher-subscriber model, multiple clients may publish data with the same topic.</span></span> <span data-ttu-id="3997f-117">Un cliente recibirá un mensaje que publica si el cliente se suscribe al mismo tema.</span><span class="sxs-lookup"><span data-stu-id="3997f-117">A client will receive a message it publishes if the client subscribes to the same topic.</span></span>

<span data-ttu-id="3997f-118">En función del caso de uso, un cliente puede elegir uno de los tres niveles de QoS al publicar un mensaje:</span><span class="sxs-lookup"><span data-stu-id="3997f-118">Depending on the use case, a client may choose one of the 3 QoS levels when publishing a message:</span></span>

- <span data-ttu-id="3997f-119">**QoS 0**: el mensaje se entrega como máximo una vez.</span><span class="sxs-lookup"><span data-stu-id="3997f-119">**QoS 0**: The message is delivered at most once.</span></span> <span data-ttu-id="3997f-120">Los mensajes enviados con QoS 0 se pueden perder.</span><span class="sxs-lookup"><span data-stu-id="3997f-120">Messages sent with QoS 0 may be lost.</span></span>
- <span data-ttu-id="3997f-121">**QoS 1**: el mensaje se entrega al menos una vez.</span><span class="sxs-lookup"><span data-stu-id="3997f-121">**QoS 1**: The message is delivered at least once.</span></span> <span data-ttu-id="3997f-122">Los mensajes enviados con QoS 1 se pueden entregar más de una vez.</span><span class="sxs-lookup"><span data-stu-id="3997f-122">Messages sent with QoS 1 may be delivered more than once.</span></span>
- <span data-ttu-id="3997f-123">**QoS 2**: el mensaje se entrega exactamente una vez.</span><span class="sxs-lookup"><span data-stu-id="3997f-123">**QoS 2**: The message is delivered exactly once.</span></span> <span data-ttu-id="3997f-124">Se garantiza la entrega de los mensajes enviados con QoS 2, sin duplicación.</span><span class="sxs-lookup"><span data-stu-id="3997f-124">Messages sent with QoS 2 is guaranteed to be delivered, with no duplication.</span></span>

> [!NOTE]
> <span data-ttu-id="3997f-125">Esta implementación de cliente MQTT no admite mensajes de nivel 2 de QoS.</span><span class="sxs-lookup"><span data-stu-id="3997f-125">This implementation of MQTT client does not support QoS level 2 messages.</span></span>

<span data-ttu-id="3997f-126">Dado que se garantiza la entrega con QoS 1 y QoS 2, el agente realiza un seguimiento del estado de los mensajes de QoS 1 y QoS 2 enviados a cada cliente.</span><span class="sxs-lookup"><span data-stu-id="3997f-126">Since QoS 1 and QoS 2 are guaranteed to be delivered, the broker keeps track the state of QoS 1 and QoS 2 messages sent to each client.</span></span> <span data-ttu-id="3997f-127">Esto es especialmente importante para los clientes que esperan mensajes de QoS 1 o QoS 2.</span><span class="sxs-lookup"><span data-stu-id="3997f-127">This is particularly important for clients that expect QoS1 or QoS 2 messages.</span></span> <span data-ttu-id="3997f-128">Es posible que el cliente esté desconectado del agente (por ejemplo, cuando se reinicie el cliente o se pierda temporalmente el vínculo de comunicación).</span><span class="sxs-lookup"><span data-stu-id="3997f-128">The client may be disconnected from the broker (for example when the client reboots, or the communication link is temporarily lost).</span></span> <span data-ttu-id="3997f-129">El agente debe almacenar los mensajes de QoS 1 y QoS 2 para que los mensajes se puedan entregar posteriormente una vez que el cliente vuelva a conectarse al agente.</span><span class="sxs-lookup"><span data-stu-id="3997f-129">The broker must store QoS 1 and QoS 2 messages so the messages can be delivered later once the client is reconnected to the broker.</span></span> <span data-ttu-id="3997f-130">Sin embargo, el cliente puede optar por no recibir ningún mensaje obsoleto del agente después de la reconexión.</span><span class="sxs-lookup"><span data-stu-id="3997f-130">However, the client may choose not to receive any stale messages from the broker after reconnection.</span></span> <span data-ttu-id="3997f-131">Para ello, puede iniciar la conexión con la marca *clean_session* establecida en \***NX_TRUE**.</span><span class="sxs-lookup"><span data-stu-id="3997f-131">The client can do so by initiating the connection with the *clean_session* flag set to \***NX_TRUE** _.</span></span> <span data-ttu-id="3997f-132">En este caso, al recibir el mensaje MQTT CONNECT, el agente descartará toda información de sesión asociada a este cliente, incluidos los mensajes de QoS 1 o QoS 2 no entregados o no confirmados.</span><span class="sxs-lookup"><span data-stu-id="3997f-132">In this case, upon receiving the MQTT CONNECT message, the broker shall discard any session information associated with this client, including undelivered or unconfirmed QoS 1 or QoS 2 messages.</span></span> <span data-ttu-id="3997f-133">Si la _marca clean_session\* está establecida en \***NX_FALSE**_, el servidor reenviará los mensajes de QoS 1 y QoS 2.</span><span class="sxs-lookup"><span data-stu-id="3997f-133">If the _clean_session\* flag is to \***NX_FALSE**_, the server shall resend the QoS 1 and QoS 2 messages.</span></span> <span data-ttu-id="3997f-134">El cliente MQTT también vuelve a enviar los mensajes no confirmados si clean_session\* está establecido en \***NX_TRUE\*.**</span><span class="sxs-lookup"><span data-stu-id="3997f-134">The MQTT Client also resends any un-acknowledged messages if _clean_session\* is set to \***NX_TRUE\*.**</span></span> <span data-ttu-id="3997f-135">Esta confirmación es diferente de la confirmación de la capa TCP, aunque también sucede.</span><span class="sxs-lookup"><span data-stu-id="3997f-135">This acknowledgment is different from the TCP layer ACK, although that happens as well.</span></span> <span data-ttu-id="3997f-136">El cliente MQTT envía una confirmación al agente.</span><span class="sxs-lookup"><span data-stu-id="3997f-136">The MQTT client sends an acknowledgment to the broker.</span></span>

<span data-ttu-id="3997f-137">Una aplicación crea una instancia del cliente MQTT mediante una llamada a \***nxd_mqtt_client_create()** .</span><span class="sxs-lookup"><span data-stu-id="3997f-137">An application creates an MQTT client instance by calling \***nxd_mqtt_client_create()** _.</span></span> <span data-ttu-id="3997f-138">Una vez creado el cliente, la aplicación puede conectarse al agente mediante una llamada a _*_nxd_mqtt_client_connect()_*_.</span><span class="sxs-lookup"><span data-stu-id="3997f-138">Once the client is created, the application can connect to the broker by calling _*_nxd_mqtt_client_connect()_*_.</span></span> <span data-ttu-id="3997f-139">Después de conectarse al agente, el cliente puede suscribirse a un tema llamando a _*_nxd_mqtt_client_subscribe()_*_ o publicar un tema llamando a \*_nxd_mqtt_client_publish()_ \*\*.</span><span class="sxs-lookup"><span data-stu-id="3997f-139">After connecting to the broker, the client can subscribe to a topic by calling _*_nxd_mqtt_client_subscribe()_*_, or publish a topic by calling _\*_nxd_mqtt_client_publish()_\*\*.</span></span>

<span data-ttu-id="3997f-140">Los mensajes MQTT entrantes se almacenan en la cola de recepción de la instancia del cliente MQTT.</span><span class="sxs-lookup"><span data-stu-id="3997f-140">Incoming MQTT messages are stored in the receive queue in the MQTT client instance.</span></span> <span data-ttu-id="3997f-141">La aplicación recupera estos mensajes mediante la llamada a ***nxd_mqtt_client_message_get()***.</span><span class="sxs-lookup"><span data-stu-id="3997f-141">Application retrieves these message by calling ***nxd_mqtt_client_message_get()***.</span></span> <span data-ttu-id="3997f-142">Si hay mensajes en la cola de recepción, el primer mensaje (por ejemplo, el más antiguo) se devuelve al autor de la llamada.</span><span class="sxs-lookup"><span data-stu-id="3997f-142">If there are messages in the receive queue, the first message (e.g. the oldest) from the queue is returned to the caller.</span></span> <span data-ttu-id="3997f-143">También se devuelve la cadena de tema del mensaje.</span><span class="sxs-lookup"><span data-stu-id="3997f-143">The topic string from the message is also returned.</span></span>

> [!NOTE]
> <span data-ttu-id="3997f-144">La función \***nxd_mqtt_client_message_get()** no se bloquea si la cola de recepción del cliente MQTT está vacía.</span><span class="sxs-lookup"><span data-stu-id="3997f-144">The function \***nxd_mqtt_client_message_get()** _ does not block if the MQTT client receive queue is empty.</span></span> <span data-ttu-id="3997f-145">La función se devuelve inmediatamente con el código de retorno _\*_NXD_MQTT_NO_MESSAGE_\*\*.</span><span class="sxs-lookup"><span data-stu-id="3997f-145">The function returns immediately with the return code _\*_NXD_MQTT_NO_MESSAGE_\*\*.</span></span> <span data-ttu-id="3997f-146">La aplicación tratará este valor devuelto como una indicación de que la cola de recepción está vacía, y no como un error.</span><span class="sxs-lookup"><span data-stu-id="3997f-146">The application shall treat this return value as an indication that the receive queue is empty, not an error.</span></span>

<span data-ttu-id="3997f-147">Para evitar el sondeo de mensajes entrantes en la cola de recepción, la aplicación puede registrar una función de devolución de llamada con el cliente MQTT mediante una llamada a ***nxd_mqtt_client_recieve_notify_set()***.</span><span class="sxs-lookup"><span data-stu-id="3997f-147">To avoid polling the receive queue for incoming messages, the application can register a callback function with the MQTT client by calling ***nxd_mqtt_client_recieve_notify_set()***.</span></span> <span data-ttu-id="3997f-148">La función de devolución de llamada se declara como:</span><span class="sxs-lookup"><span data-stu-id="3997f-148">The callback function is declared as:</span></span>

```c
VOID (*receive_notify_callback)(NXD_MQTT_CLIENT *client_ptr, 
    UINT message_count);
```

<span data-ttu-id="3997f-149">Cuando el cliente MQTT recibe mensajes del agente, invoca la función de devolución de llamada si la función está establecida.</span><span class="sxs-lookup"><span data-stu-id="3997f-149">As the MQTT client receives messages from the broker, it invokes the callback function if the function is set.</span></span> <span data-ttu-id="3997f-150">La función de devolución de llamada pasa el puntero al bloque de control del cliente y un valor de recuento de mensajes.</span><span class="sxs-lookup"><span data-stu-id="3997f-150">The callback function passes the pointer to the client control block and a message count value.</span></span> <span data-ttu-id="3997f-151">El valor de recuento de mensajes indica el número de mensajes MQTT en la cola de recepción.</span><span class="sxs-lookup"><span data-stu-id="3997f-151">The message count value indicates the number of MQTT messages in the receive queue.</span></span> <span data-ttu-id="3997f-152">Tenga en cuenta que esta función de devolución de llamada se ejecuta en el contexto del subproceso de cliente MQTT.</span><span class="sxs-lookup"><span data-stu-id="3997f-152">Note that this callback function executes in the MQTT client thread context.</span></span> <span data-ttu-id="3997f-153">Por consiguiente, la función de devolución de llamada no debe ejecutar ningún procedimiento que pueda bloquear el subproceso de cliente MQTT.</span><span class="sxs-lookup"><span data-stu-id="3997f-153">Therefore, the callback function should not execute any procedures that may block the MQTT client thread.</span></span> <span data-ttu-id="3997f-154">La función de devolución de llamada debe desencadenar el subproceso de la aplicación para llamar a ***nxd_mqtt_client_message_get()*** y recuperar los mensajes.</span><span class="sxs-lookup"><span data-stu-id="3997f-154">The callback function should trigger the application thread to call ***nxd_mqtt_client_message_get()*** to retrieve the messages.</span></span>

<span data-ttu-id="3997f-155">Para desconectar y finalizar el servicio del cliente MQTT, la aplicación debe usar el servicio ***nxd_mqtt_client_disconnect()** y _*_nxd_mqtt_client_delete()._\*_</span><span class="sxs-lookup"><span data-stu-id="3997f-155">To disconnect and terminate the MQTT client service, the application shall use the service ***nxd_mqtt_client_disconnect()** _ and _*_nxd_mqtt_client_delete()._\*_</span></span> <span data-ttu-id="3997f-156">La llamada a _*_nxd_mqtt_client_disconnect()_*_ simplemente desconecta la conexión TCP al agente.</span><span class="sxs-lookup"><span data-stu-id="3997f-156">Calling _*_nxd_mqtt_client_disconnect()_*_ simply disconnects the TCP connection to the broker.</span></span> <span data-ttu-id="3997f-157">Los mensajes ya recibidos y almacenados en la cola de recepción se liberan.</span><span class="sxs-lookup"><span data-stu-id="3997f-157">It releases messages already received and stored in the receive queue.</span></span> <span data-ttu-id="3997f-158">Sin embargo, no se publican mensajes de nivel 1 de QoS en la cola de transmisión.</span><span class="sxs-lookup"><span data-stu-id="3997f-158">However, it does not release QoS level 1 messages in the transmit queue.</span></span> <span data-ttu-id="3997f-159">Los mensajes de QoS de nivel 1 se retransmiten tras la conexión, suponiendo que la marca _*_clean_session_*_ está establecida en _ *_NX_FALSE._*\*</span><span class="sxs-lookup"><span data-stu-id="3997f-159">QoS level 1 messages are retransmitted upon connection, assuming the _*_clean_session_*_ flag is set to _ *_NX_FALSE._*\*</span></span>

<span data-ttu-id="3997f-160">El agente también puede desconectarse del cliente.</span><span class="sxs-lookup"><span data-stu-id="3997f-160">The broker may also disconnect from the client.</span></span> <span data-ttu-id="3997f-161">Cuando finaliza la conexión TCP entre el cliente y el agente, se puede notificar a la aplicación mediante la función de notificación de desconexión.</span><span class="sxs-lookup"><span data-stu-id="3997f-161">When the TCP connection between the client and the broker is terminated, the application can be notified by the disconnect notify function.</span></span> <span data-ttu-id="3997f-162">Para usar el mecanismo de notificación, la aplicación instala la función de notificación de desconexión mediante una llamada a \***nxd_mqtt_client_disconnect_notify_set\*.**</span><span class="sxs-lookup"><span data-stu-id="3997f-162">To use the notification mechanism, application installs the disconnect notify function by calling \***nxd_mqtt_client_disconnect_notify_set\*.**</span></span> <span data-ttu-id="3997f-163">Una vez que se observa una desconexión TCP y se crea la sesión MQTT, se invoca la función de notificación.</span><span class="sxs-lookup"><span data-stu-id="3997f-163">Once a TCP disconnect is observed and the MQTT session has been created, the notification function is invoked.</span></span>

<span data-ttu-id="3997f-164">La llamada a ***nxd_mqtt_client_delete()*** libera todos los bloques de mensajes de la cola de transmisión y la cola de recepción.</span><span class="sxs-lookup"><span data-stu-id="3997f-164">Calling ***nxd_mqtt_client_delete()*** releases all message blocks in the transmit queue and the receive queue.</span></span> <span data-ttu-id="3997f-165">También se eliminan los mensajes de nivel 1 de QoS no confirmados.</span><span class="sxs-lookup"><span data-stu-id="3997f-165">Unacknowledged QoS level 1 messages are also deleted.</span></span>

## <a name="secure-mqtt-connection"></a><span data-ttu-id="3997f-166">Conexión MQTT segura</span><span class="sxs-lookup"><span data-stu-id="3997f-166">Secure MQTT Connection</span></span>

<span data-ttu-id="3997f-167">El cliente MQTT establece una conexión segura con el agente mediante el módulo NetX Secure TLS.</span><span class="sxs-lookup"><span data-stu-id="3997f-167">The MQTT client makes a secure connection to the broker using the NetX Secure TLS module.</span></span> <span data-ttu-id="3997f-168">El número de puerto predeterminado de MQTT con seguridad TLS es 8883, definido en ***NXD_MQTT_TLS_PORT***.</span><span class="sxs-lookup"><span data-stu-id="3997f-168">The default port number for MQTT with TLS security is 8883, defined in ***NXD_MQTT_TLS_PORT***.</span></span>

<span data-ttu-id="3997f-169">Para crear una conexión MQTT segura con el agente, es necesario negociar una sesión TLS después de establecer una conexión TCP para que se puedan enviar mensajes MQTT CONNECT al agente.</span><span class="sxs-lookup"><span data-stu-id="3997f-169">To create a secure MQTT connection to the broker, a TLS session needs to be negotiated after a TCP connection is established, before MQTT CONNECT messages can be sent to the broker.</span></span> <span data-ttu-id="3997f-170">La configuración de la sesión TLS se realiza llamando a ***nxd_mqtt_client_secure_connect()*** y pasando una función de devolución de llamada de configuración TLS definida por el usuario.</span><span class="sxs-lookup"><span data-stu-id="3997f-170">The TLS session set up is accomplished by calling ***nxd_mqtt_client_secure_connect()*** and passing in a user-defined TLS setup callback function.</span></span> <span data-ttu-id="3997f-171">Durante la fase de conexión de MQTT, una vez establecida la conexión TCP, el cliente invoca la función de devolución de llamada de configuración de TLS para iniciar un proceso de enlace de TLS adecuado.</span><span class="sxs-lookup"><span data-stu-id="3997f-171">During the MQTT connection phase, once the TCP connection is established, the client invokes the TLS setup callback function to start a proper TLS handshake process.</span></span> <span data-ttu-id="3997f-172">Una vez establecida la sesión TLS, el cliente continúa el mensaje MQTT CONNECT a través del canal seguro.</span><span class="sxs-lookup"><span data-stu-id="3997f-172">After the TLS session is established, the client continues the MQTT CONNECT message over the secure channel.</span></span>

<span data-ttu-id="3997f-173">La función de devolución de llamada definida por el usuario toma cinco valores de entrada y se declara como:</span><span class="sxs-lookup"><span data-stu-id="3997f-173">The user defined callback function takes five input values and is declared as:</span></span>

```c
UINT tls_Setup_callback(NXD_MQTT_CLIENT *client_ptr,
    NX_SECURE_TLS_SESSION *session_ptr,
    NX_SECURE_TLS_CERTIFICATE *certificate_ptr,
    NX_SECURE_TLS_CERTIFICATE *trusted_cerfiticate);
```

<span data-ttu-id="3997f-174">A continuación, se muestra una descripción de los parámetros de entrada:</span><span class="sxs-lookup"><span data-stu-id="3997f-174">Below is a description of the input parameters:</span></span>

- <span data-ttu-id="3997f-175">**client_ptr** Puntero al bloque de control del cliente MQTT.</span><span class="sxs-lookup"><span data-stu-id="3997f-175">**client_ptr**: Pointer to the MQTT client control block.</span></span>
- <span data-ttu-id="3997f-176">**session_ptr** Puntero al bloque de control de sesión de TLS.</span><span class="sxs-lookup"><span data-stu-id="3997f-176">**session_ptr**: Pointer to the TLS session control block.</span></span>
- <span data-ttu-id="3997f-177">**certificate_ptr** Puntero al bloque de control de certificado.</span><span class="sxs-lookup"><span data-stu-id="3997f-177">**certificate_ptr**: Pointer to the certificate control block.</span></span> <span data-ttu-id="3997f-178">La función de instalación configura este certificado antes de enviarlo al agente.</span><span class="sxs-lookup"><span data-stu-id="3997f-178">The setup function configures this certificate before sending it to the broker.</span></span>
- <span data-ttu-id="3997f-179">**trusted_certificate_ptr** Puntero al certificado de confianza.</span><span class="sxs-lookup"><span data-stu-id="3997f-179">**trusted_certificate_ptr**: Pointer to the trusted certificate.</span></span> <span data-ttu-id="3997f-180">La función de configuración de TLS configura el certificado de confianza para autenticar el servidor.</span><span class="sxs-lookup"><span data-stu-id="3997f-180">TLS setup function configures the trusted certificate to authenticate the server.</span></span>

<span data-ttu-id="3997f-181">En la función de configuración de TLS, la aplicación es responsable de crear una sesión TLS y de configurar la sesión con un certificado adecuado.</span><span class="sxs-lookup"><span data-stu-id="3997f-181">In the TLS setup function, the application is responsible for creating a TLS session, and configuring the session with a proper certificate.</span></span> <span data-ttu-id="3997f-182">En el siguiente pseudocódigo se describe un procedimiento de inicio de sesión TLS típico.</span><span class="sxs-lookup"><span data-stu-id="3997f-182">The following pseudo code outlines a typical TLS session start up procedure.</span></span> <span data-ttu-id="3997f-183">Para más información sobre el uso de las API de TLS, se hace referencia al lector en la guía de usuario de NetX Secure TLS.</span><span class="sxs-lookup"><span data-stu-id="3997f-183">The reader is referred to the NetX Secure TLS User Guide for details on using TLS APIs.</span></span>

<span data-ttu-id="3997f-184">A continuación, se muestra un ejemplo de devolución de llamada de configuración de TLS:</span><span class="sxs-lookup"><span data-stu-id="3997f-184">Below is an example TLS setup callback:</span></span>

```c
UINT tls_setup_callback(NXD_MQTT_CLIENT *client_pt
    NX_SECURE_TLS_SESSION *session_ptr,
    NX_SECURE_TLS_CERTIFICATE *certrifcate_ptr,
    NX_SECURE_TLS_CERTIFICATE *trusted_certificate_ptr)
{
    /* Initialize TLS module */
    nx_secure_tls_initialize();

    /* Create a TLS session */
    nx_secure_tls_session_create(session_ptr, …);

    /* Need to allocate space for the certificate coming in from the broker. */
    memset(certificate_ptr), 0, sizeof(NX_SECURE_TLS_CERTIFICATE));

    nx_secure_tls_remote_certificate_allocate(session_ptr, certificate_ptr);

    /* Add a CA Certificate to our trusted store for verifying incomingserver certificates. */
    nx_secure_tls_certificate_initialize(
        trusted_certificate_ptr,
        ca_cert_der,
        ca_cert_der_len, NULL, 0);
    nx_secure_tls_trusted_certificate_add(session_ptr,
        trusted_certificate));
}
```

## <a name="known-limitations-of-the-netx-duo-mqtt-client"></a><span data-ttu-id="3997f-185">Limitaciones conocidas del cliente MQTT de NetX Duo</span><span class="sxs-lookup"><span data-stu-id="3997f-185">Known Limitations of the NetX Duo MQTT Client</span></span>

- <span data-ttu-id="3997f-186">MQTT de NetX Duo no admite el envío ni la recepción de mensajes de nivel 2 de QoS.</span><span class="sxs-lookup"><span data-stu-id="3997f-186">NetX Duo MQTT does not support sending or receiving QoS level 2 messages.</span></span>
- <span data-ttu-id="3997f-187">NetX Duo MQTT no admite paquetes encadenados.</span><span class="sxs-lookup"><span data-stu-id="3997f-187">NetX Duo MQTT does not support chained-packets.</span></span>
