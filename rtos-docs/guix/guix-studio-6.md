---
title: Código generado por GUIX Studio
description: Cuando haya terminado de editar las pantallas y los recursos, GUIX Studio generará un conjunto de archivos de salida que se podrán incorporar a la aplicación insertada.
author: philmea
ms.author: philmea
ms.date: 5/19/2020
ms.service: rtos
ms.topic: article
ms.openlocfilehash: f8868ec770aa8f7f35d2866b99e3eb8f501281a8
ms.sourcegitcommit: e3d42e1f2920ec9cb002634b542bc20754f9544e
ms.translationtype: HT
ms.contentlocale: es-ES
ms.lasthandoff: 03/22/2021
ms.locfileid: "104815473"
---
# <a name="chapter-6-guix-studio-generated-code"></a><span data-ttu-id="362a1-103">Capítulo 6: Código generado por GUIX Studio</span><span class="sxs-lookup"><span data-stu-id="362a1-103">Chapter 6: GUIX Studio Generated Code</span></span>

<span data-ttu-id="362a1-104">Cuando haya terminado de editar las pantallas y los recursos, GUIX Studio generará un conjunto de archivos de salida que se podrán incorporar a la aplicación insertada.</span><span class="sxs-lookup"><span data-stu-id="362a1-104">When you are done editing your screens and resources, GUIX Studio produces a set of output files that can be incorporated into your embedded application.</span></span> <span data-ttu-id="362a1-105">Para generar los archivos de salida, seleccione ***Generate Resource Files** _ (Generar archivos de recursos) y _ *_Generate Specifications_** (Generar especificaciones) en el elemento de menú Project (Proyecto).</span><span class="sxs-lookup"><span data-stu-id="362a1-105">The output files are generated by selecting ***Generate Resource Files** _ and _ *_Generate Specifications_** from the Project menu item.</span></span> <span data-ttu-id="362a1-106">Los archivos de código fuente del lenguaje “C” generados por GUIX Studio están diseñados para compilarse y vincularse con el código fuente de la aplicación insertada.</span><span class="sxs-lookup"><span data-stu-id="362a1-106">The 'c' language source code files generated by GUIX Studio are intended to be compiled and linked with the embedded application source code.</span></span> <span data-ttu-id="362a1-107">Si se genera un archivo de recursos de formato binario, este se debe programar en un área de almacenamiento no volátil en el destino, y la función de la API de GUIX gx_binres_theme_install debe usarse para instalar los recursos binarios en el tiempo de ejecución.</span><span class="sxs-lookup"><span data-stu-id="362a1-107">If a binary format resource file is produced, this file should be programmed to a non-volatile storage area on the target and the GUIX API function gx_binres_theme_install should be used to install the binary resources at runtime.</span></span>

<span data-ttu-id="362a1-108">El código de aplicación insertada del usuario hace referencia al código generado por GUIX Studio.</span><span class="sxs-lookup"><span data-stu-id="362a1-108">The user's embedded application code makes references to the code generated by GUIX Studio.</span></span> <span data-ttu-id="362a1-109">Además, el código generado por GUIX Studio espera que todas las funciones personalizadas de dibujo de widgets, control de eventos y asignación de memoria especificadas en el proyecto se definan en el código de aplicación insertada del usuario.</span><span class="sxs-lookup"><span data-stu-id="362a1-109">Furthermore, the GUIX Studio generated code expects all custom widget drawing, event handling, and memory allocation functions specified in the project to be defined in the user's embedded application code.</span></span> <span data-ttu-id="362a1-110">Si no es así, habrá errores de vínculo al crear la aplicación.</span><span class="sxs-lookup"><span data-stu-id="362a1-110">If they are not, link errors will be present when building the application.</span></span>

> [!NOTE]
> <span data-ttu-id="362a1-111">El usuario nunca debería tener que modificar el código generado por GUIX Studio, y debe evitar hacerlo.</span><span class="sxs-lookup"><span data-stu-id="362a1-111">The user should never have to modify the code generated by GUIX Studio and should resist doing so.</span></span> <span data-ttu-id="362a1-112">Todas las modificaciones de la interfaz de usuario deben realizarse en el proyecto de GUIX Studio asociado.</span><span class="sxs-lookup"><span data-stu-id="362a1-112">All UI modifications should be made in the associated GUIX Studio project.</span></span> <span data-ttu-id="362a1-113">Así se mantendrá el proyecto sincronizado con la aplicación insertada.</span><span class="sxs-lookup"><span data-stu-id="362a1-113">This will keep the project synchronized with the embedded application.</span></span>

## <a name="generating-resource-files"></a><span data-ttu-id="362a1-114">Generación de archivos de recursos</span><span class="sxs-lookup"><span data-stu-id="362a1-114">Generating Resource Files</span></span>

<span data-ttu-id="362a1-115">Los archivos de recursos generados por GUIX Studio contienen estructuras de datos preestablecidas que definen todos los recursos de GUIX Studio (colores, fuentes, mapas de píxeles y cadenas), que son, en la práctica, todos los recursos definidos en la sección ***Resource View*** (Vista de recursos) del proyecto.</span><span class="sxs-lookup"><span data-stu-id="362a1-115">Resource files generated by GUIX Studio contain preset data structures that define all of the GUIX Studio resources (colors, fonts, pixelmaps, and strings), which is effectively all the resources defined in the ***Resource View*** of the project.</span></span> <span data-ttu-id="362a1-116">Estos archivos de recursos se pueden generar en código fuente o en formatos binarios.</span><span class="sxs-lookup"><span data-stu-id="362a1-116">These resource files can be generated in source code or binary forms.</span></span>

<span data-ttu-id="362a1-117">De forma predeterminada, se generan dos archivos: un archivo es de código fuente estándar de C; el otro es un archivo de encabezado de C que proporciona referencias externas y constantes que son necesarias para que el código de la aplicación acceda a los recursos de GUIX definidos en el proyecto.</span><span class="sxs-lookup"><span data-stu-id="362a1-117">By default, there are two files generated, one file is a standard C source code file and the other is a C header file that provides external references and constants that are necessary for the application code to access the GUIX resources defined in the project.</span></span> <span data-ttu-id="362a1-118">Los nombres de archivo tienen el siguiente formato:</span><span class="sxs-lookup"><span data-stu-id="362a1-118">The file names are of the form:</span></span>

<span data-ttu-id="362a1-119">**{*project-name*}_resources.h**</span><span class="sxs-lookup"><span data-stu-id="362a1-119">**{*project-name*}_resources.h**</span></span>

<span data-ttu-id="362a1-120">**{*project-name*}_resources.c**</span><span class="sxs-lookup"><span data-stu-id="362a1-120">**{*project-name*}_resources.c**</span></span>

<span data-ttu-id="362a1-121">Por ejemplo, los archivos de recursos creados para el proyecto de GUIX Studio "***simple***" son:</span><span class="sxs-lookup"><span data-stu-id="362a1-121">For example, the Resource files created for the "***simple***" GUIX Studio project are:</span></span>

<span data-ttu-id="362a1-122">**simple_resources.h**</span><span class="sxs-lookup"><span data-stu-id="362a1-122">**simple_resources.h**</span></span>

<span data-ttu-id="362a1-123">**simple_resources.c**</span><span class="sxs-lookup"><span data-stu-id="362a1-123">**simple_resources.c**</span></span>

<span data-ttu-id="362a1-124">Para generar los archivos de recursos, seleccione la opción ***Generate Resource Files** _ (Generar archivos de recursos) en la opción de menú _*_Project_\*_ (Proyecto).</span><span class="sxs-lookup"><span data-stu-id="362a1-124">Generating the Resource files is accomplished by selecting ***Generate Resource Files** _ option in the _*_Project_\*_ menu option.</span></span> <span data-ttu-id="362a1-125">El destino de los archivos de recursos se especifica en el cuadro de diálogo _*_Configure Project_*_ (Configurar proyecto), al que se puede acceder a través de la opción _*_Configure Project/Displays_*_ (Configurar proyecto/paneles) del elemento de menú _ *_Configure_*\* (Configurar).</span><span class="sxs-lookup"><span data-stu-id="362a1-125">The destination of the resource files is specified in the _*_Configure Project_*_ dialog, which is accessible via the _*_Configure Project/Displays_*_ option in the _ *_Configure_*\* menu item.</span></span>

<span data-ttu-id="362a1-126">En el caso de los recursos de mapas de píxeles y fuentes, puede especificar un nombre de archivo de salida personalizado para cada mapa de píxeles y fuente en los cuadros de diálogo de edición de recursos asociados.</span><span class="sxs-lookup"><span data-stu-id="362a1-126">For Pixelmap and Font resources, you can specify a custom output filename for each pixelmap and font in the associated resource editing dialogs.</span></span> <span data-ttu-id="362a1-127">Esta característica permite colocar recursos muy grandes en archivos distintos, en lugar de colocar todos los recursos en un solo archivo de salida común.</span><span class="sxs-lookup"><span data-stu-id="362a1-127">This feature allows you to put very large resources in distinct files, rather than putting all resources in one common output file.</span></span> <span data-ttu-id="362a1-128">Si no especifica ningún nombre de archivo invalidado para un recurso de fuente o mapa de píxeles, esos recursos se escriben en el archivo de recursos común.</span><span class="sxs-lookup"><span data-stu-id="362a1-128">If you do not specify an overridden filename for a font or pixelmap resource, those resources are written into the common resource file.</span></span>

<span data-ttu-id="362a1-129">Si prefiere usar recursos binarios, puede especificar un formato de salida de s-record o raw.</span><span class="sxs-lookup"><span data-stu-id="362a1-129">If you prefer to use binary resources, you can specify either raw or standard s-record output format.</span></span> <span data-ttu-id="362a1-130">Los recursos binarios no se compilan ni vinculan con el código de la aplicación, sino que se cargan en tiempo de ejecución mediante la API gx_binres_them_load().</span><span class="sxs-lookup"><span data-stu-id="362a1-130">Binary resources are not compiled or linked with the application code, but are instead loaded at runtime using the gx_binres_them_load() API.</span></span> <span data-ttu-id="362a1-131">Este servicio de API crea tablas de recursos que dirigen a los recursos almacenados en memoria no volátil.</span><span class="sxs-lookup"><span data-stu-id="362a1-131">This API service builds resource tables that point to your resources stored in non-volatile memory.</span></span> <span data-ttu-id="362a1-132">Después, puede instalar estos recursos con un panel determinado mediante gx_display_theme_install().</span><span class="sxs-lookup"><span data-stu-id="362a1-132">You can then install these resources with a particular display using gx_display_theme_install();</span></span>

## <a name="generating-specification-code"></a><span data-ttu-id="362a1-133">Generación del código de especificación</span><span class="sxs-lookup"><span data-stu-id="362a1-133">Generating Specification Code</span></span>

<span data-ttu-id="362a1-134">Los archivos de especificación generados por GUIX Studio contienen todo el código C para crear la interfaz de usuario diseñada en GUIX Studio.</span><span class="sxs-lookup"><span data-stu-id="362a1-134">The Specification files generated by GUIX Studio contain all the C code to create the UI designed in GUIX Studio.</span></span> <span data-ttu-id="362a1-135">Este código también hace referencia a los archivos de recursos generados para este proyecto.</span><span class="sxs-lookup"><span data-stu-id="362a1-135">This code also references the Resource files generated for this project.</span></span> <span data-ttu-id="362a1-136">El código de aplicación del usuario realizará llamadas a este código para crear realmente los objetos de interfaz de usuario definidos en el proyecto.</span><span class="sxs-lookup"><span data-stu-id="362a1-136">The user's application code will make calls to this code to actually create the UI objects defined in the project.</span></span> <span data-ttu-id="362a1-137">Además, el código de aplicación del usuario contiene todas las funciones personalizadas de dibujo de widgets, control de eventos y asignación de memoria que se especifiquen en el proyecto.</span><span class="sxs-lookup"><span data-stu-id="362a1-137">Furthermore, the user's application code contains all custom widget drawing, event handling, and memory allocation functions specified in the project.</span></span> <span data-ttu-id="362a1-138">De forma predeterminada, se generan dos archivos: un archivo es de código fuente estándar de C; el otro es un archivo de encabezado de C que proporciona referencias externas y constantes que son necesarias para que el código de la aplicación acceda a las especificaciones de GUIX Studio.</span><span class="sxs-lookup"><span data-stu-id="362a1-138">By default, there are two files generated, one file is a standard C source code file and the other is a C header file that provides external references and constants that are necessary for the application code to access the GUIX Studio Specifications.</span></span> <span data-ttu-id="362a1-139">Los nombres de archivo tienen el siguiente formato:</span><span class="sxs-lookup"><span data-stu-id="362a1-139">The file names are of the form:</span></span>

<span data-ttu-id="362a1-140">**{*project-name*}_specifications.h**</span><span class="sxs-lookup"><span data-stu-id="362a1-140">**{*project-name*}_specifications.h**</span></span>

<span data-ttu-id="362a1-141">**{*project-name*}_specifications.c**</span><span class="sxs-lookup"><span data-stu-id="362a1-141">**{*project-name*}_specifications.c**</span></span>

<span data-ttu-id="362a1-142">Por ejemplo, los archivos de especificaciones creados para el proyecto de GUIX Studio "***simple***" son:</span><span class="sxs-lookup"><span data-stu-id="362a1-142">For example, the Specification files created for the "***simple***" GUIX Studio project are:</span></span>

<span data-ttu-id="362a1-143">**simple_specifications.h**</span><span class="sxs-lookup"><span data-stu-id="362a1-143">**simple_specifications.h**</span></span>

<span data-ttu-id="362a1-144">**simple_specifications.c**</span><span class="sxs-lookup"><span data-stu-id="362a1-144">**simple_specifications.c**</span></span>

<span data-ttu-id="362a1-145">Para generar los archivos de especificaciones, seleccione la opción ***Generate Specification Files** _ (Generar archivos de especificación) que hay en la opción de menú _*_Project_\*_ (Proyecto).</span><span class="sxs-lookup"><span data-stu-id="362a1-145">Generating the Specification files is accomplished by selecting ***Generate Specification Files** _ option in the _*_Project_\*_ menu option.</span></span> <span data-ttu-id="362a1-146">El destino de los archivos de especificaciones se especifica en el cuadro de diálogo _*_Configure Project_*_ (Configurar proyecto), al que se puede acceder a través de la opción _*_Configure Project/Displays_*_ (Configurar proyecto/paneles) del elemento de menú _ *_Configure_*\* (Configurar).</span><span class="sxs-lookup"><span data-stu-id="362a1-146">The destination of the Specification files is specified in the _*_Configure Project_*_ dialog, which is accessible via the _*_Configure Project/Displays_*_ option in the _ *_Configure_*\* menu item.</span></span>

## <a name="integrating-with-user-code"></a><span data-ttu-id="362a1-147">Integración con el código de usuario</span><span class="sxs-lookup"><span data-stu-id="362a1-147">Integrating with User Code</span></span>

<span data-ttu-id="362a1-148">Es sencillo integrar los archivos de recursos y especificaciones generados por GUIX Studio; tan solo siga estos pasos:</span><span class="sxs-lookup"><span data-stu-id="362a1-148">Integrating the Resource and Specification files generated by GUIX Studio is straightforward, simply follow these steps:</span></span>

1. <span data-ttu-id="362a1-149">Copie o haga que los archivos de recursos y especificaciones sean accesibles a través de la configuración de la ruta de acceso al entorno de compilación insertado.</span><span class="sxs-lookup"><span data-stu-id="362a1-149">Either copy or make the Resource and Specification files accessible via path settings to the embedded build environment</span></span>
2. <span data-ttu-id="362a1-150">Agregue todos los archivos de recursos y especificaciones al proyecto IDE o archivo Make insertados.</span><span class="sxs-lookup"><span data-stu-id="362a1-150">Add all Resource and Specification files to embedded IDE project or makefile</span></span>
3. <span data-ttu-id="362a1-151">Asegúrese de que el código insertado de la aplicación llama a las funciones necesarias para inicializar y crear la interfaz de usuario contenida en los archivos de recursos y especificaciones.</span><span class="sxs-lookup"><span data-stu-id="362a1-151">Ensure the application embedded code calls the necessary functions to initialize and create the UI contained in the Resource and Specification files</span></span>
4. <span data-ttu-id="362a1-152">Asegúrese de que el código insertado de la aplicación contiene todas las funciones personalizadas necesarias de dibujo de widgets, control de eventos y asignación de memoria.</span><span class="sxs-lookup"><span data-stu-id="362a1-152">Ensure the application embedded code contains all necessary custom widget drawing, event handling, and memory allocation functions</span></span>
5. <span data-ttu-id="362a1-153">Cree la aplicación (compile y vincule).</span><span class="sxs-lookup"><span data-stu-id="362a1-153">Build the application (compile and link)</span></span>
6. <span data-ttu-id="362a1-154">Ejecute la aplicación.</span><span class="sxs-lookup"><span data-stu-id="362a1-154">Execute the application!</span></span>